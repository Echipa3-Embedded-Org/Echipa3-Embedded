import React from 'react';
import { Typography, Container } from '@mui/material';

const Dashboard = () => {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>Dashboard</Typography>
      <Typography>Imagini live vor apărea aici.</Typography>
    </Container>
  );
};

export default Dashboard;
