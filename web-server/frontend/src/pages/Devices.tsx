import React from 'react';
import { Typography, Container } from '@mui/material';

const Devices = () => {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>Dispozitive</Typography>
      <Typography>Lista cu dispozitive conectate va fi aici.</Typography>
    </Container>
  );
};

export default Devices;
