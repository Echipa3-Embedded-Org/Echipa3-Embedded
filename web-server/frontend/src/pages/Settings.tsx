import React from 'react';
import { Typography, Container } from '@mui/material';

const Settings = () => {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>Setări</Typography>
      <Typography>Aici vei putea configura aplicația.</Typography>
    </Container>
  );
};

export default Settings;
